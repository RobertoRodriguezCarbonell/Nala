# Normaliza los finales de línea: LF en el repositorio y en el working tree,
# para evitar avisos de CRLF en Windows (core.autocrlf).
* text=auto eol=lf

# Binarios: que Git no toque sus finales de línea.
*.png binary
*.ico binary
*.svg text eol=lf
